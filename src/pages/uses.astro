---
import Layout from '../layouts/Layout.astro'
import UsesItem from '../components/Uses-Item.astro'
import uses from '../data/uses.json'
---

<Layout title="Uses | Chris Tse">
	<main class="flex-1 w-full px-4 py-8 mx-auto md:px-8 md:py-8">
		<h1 class="text-2xl font-bold mb-8">
			Uses
		</h1>

		<p class="mb-8">Here is a list of all the things I use, from editor themes to PC hardware.</p>

		{uses.map((section) => (
			<section class="mb-4">
				<h2 class="text-xl font-bold mb-2 pl-1 border-b-2">{section.section}</h2>
				{section.entries.map((entry, index) => (
					<UsesItem {...entry} />
				))}
			</section>
		))}
	</main>
</Layout>
