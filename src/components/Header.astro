---
import Link from './Link.astro'
const routeList = [
	{
		route: `/work`,
		title: `Work`
	},
	{
		route: `/projects`,
		title: `Projects`
	},
	{
		route: `/uses`,
		title: `Uses`
	},
	{
		route: `/blog`,
		title: `Blog`
	},
	{
		route: `/contact`,
		title: `Contact`
	}]
---

<header class="text-purple-900">
	<div class="flex flex-wrap items-center justify-between max-w-3xl p-4 mx-auto md:p-8">
		<Link href="/">
        <span class="flex items-center no-underline">
            <img width="50" src="/logo.png" alt="Chris Tse Logo" />
        </span>
		</Link>

		<button
			id="menuToggle"
			class="flex items-center px-3 py-2 border border-white rounded md:hidden"

		>
			<svg class="w-3 h-3 fill-current" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
				<title>Menu</title>
				<path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
			</svg>
		</button>

		<nav
			id="menu"
			class="sm:hidden md:flex md:items-center w-full md:w-auto md:shadow-none shadow-md p-4 md:mt-0 mt-2"
		>
			{routeList.map((link) => (
				<a
					class="no-underline font-normal mb-4 md:mb-0 block md:inline-block md:mt-0 md:ml-6"
					href={link.route}
				>
					{link.title}
				</a>
			))}
		</nav>
	</div>
</header>

<script>
	const menuToggle = document.getElementById('menuToggle')
	const menu = document.getElementById('menu')

	menuToggle?.addEventListener('click', () => {
		menu?.classList.toggle('sm:hidden')
	})

	const currentPath = window.location.pathname
	console.log(currentPath)
	const links = document.querySelectorAll('#menu a')
	links.forEach(link => {
		if (currentPath.includes(link.getAttribute('href') ?? '')) {
			link.classList.remove('no-underline', 'font-normal')
			link.classList.add('underline', 'underline-offset-4', 'font-semibold')
		}
	})
</script>

<style>
  .header {
    color: var(--color-primary)
  }
</style>
